# 版本记录

## v0.1.0 - 2026-09-18

状态：静态聊天界面阶段已通过验收。

已完成：

- React + Vite 前端项目
- Node.js + Express 后端项目
- 「森月居」名称与「枭」AI 角色
- 清冷浅灰蓝、蓝粉渐变的视觉风格
- 居中的圆角聊天内容区域
- 会话侧边栏
- 消息展示区
- 输入框与发送按钮
- 新建会话和切换会话
- 消息在当前页面临时显示
- 响应式布局
- 后端健康检查接口
- AI 头像统一配置，预留后续自定义图片的位置
- 「森月居」标题优先使用 Huiwen-MinchoGBK 字体
- 中文 README 启动说明

明确未包含：

- AI 模型调用
- API 密钥
- 数据库
- 长期记忆
- 登录或用户系统
- 云端部署
- 付费服务

下一阶段范围：

- 只实现最小的一问一答
- 一次请求对应一次回复
- 不保存历史记录
- 不加入数据库
- 不加入长期记忆

## v0.2.0 - 2026-09-18

状态：最小一问一答链路已完成，当前使用固定占位回应。

已完成：

- 前端发送单次消息到后端
- Vite 开发代理连接前后端
- 后端 `POST /api/chat` 接口
- 每次请求只接收当前一句话
- 后端返回一次占位回应
- 前端显示等待回应状态
- 后端未启动时显示明确提示

明确未包含：

- AI 模型调用
- API 密钥
- 数据库
- 长期记忆
- 多轮上下文
- 登录或用户系统

## v0.3.0 - 2026-09-18

状态：Claude 单次问答代码已接入，等待使用者在本地 `backend/.env` 填写真实配置后完成在线测试。

已完成：

- 后端从环境变量读取 `MODEL_API_KEY`、`MODEL_BASE_URL` 和 `MODEL_NAME`
- 后端调用 OpenAI 兼容的 `/v1/chat/completions` 接口
- 通过完整模型 ID 选择 Claude Thinking 模型
- 系统提示词保存在后端配置文件中，前端无法访问
- 最大回复长度限制为 256 tokens，后端硬上限为 512
- 前端只向 `/api/chat` 发送当前这一句话
- 后端只返回一次文本回复
- 健康检查显示模型配置状态，不调用模型
- `.env.example` 不包含真实值
- `.env` 已确认被 Git 忽略

明确未包含：

- 数据库
- 长期记忆
- 多轮上下文
- 多模型切换
- 登录或用户系统
- 前端保存 API Key

## v0.4.0 - 2026-09-18

状态：Supabase 单用户持久化已接入，等待使用者创建 Supabase 项目、执行迁移并填写后端环境变量后完成在线验收。

已完成：

- 创建 `sessions` 和 `messages` 两张核心表迁移
- 为两张表启用 RLS 和所有者隔离策略
- 单用户 Supabase Auth 登录
- 后端使用用户访问令牌访问数据表
- 新建会话、加载历史、重命名会话、发送消息、删除会话均经过后端
- 用户消息先显示，模型回复返回后追加
- 等待期间禁止重复提交
- 模型或保存失败后恢复用户输入，允许重试
- 消息和回复都保存成功后才结束加载状态
- 新消息自动滚动，长文本和代码自动换行
- 重命名使用应用内编辑框，不依赖浏览器弹窗
- 删除前二次确认，删除后自动切换到仍存在的会话或欢迎页
- 删除会话时级联删除消息
- 数据库和模型失败均返回可理解的前端提示
- 后端 Secret Key、模型 Key 和系统提示词不进入前端
- 没有设置表、记忆压缩或长期摘要

明确未包含：

- 多用户注册
- 记忆压缩
- 长期摘要
- 多模型切换
- 云端部署
